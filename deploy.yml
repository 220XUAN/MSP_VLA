# Basic experiment configuration
policy_name: Pi_05
protocol: ws
host: localhost
bench_name: null
task_name: null
env_cfg_type: null
seed: null
eval_batch: true

checkpoint_num: 29999
result_dir: "./results/pi05_msp_stage2/"
obs_transform_pipeline: "xspark-v1.0"

# Model / action configuration
action_type: joint
policy_uv_env_path: openpi
# Must match the config used by train.sh (default OPENPI_TRAIN_CONFIG_NAME).
train_config_name: pi05_msp_stage2_aloha_arx-x5_seed_0
repo_id: arx_x5_sim

port: 6000
